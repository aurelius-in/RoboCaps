apiVersion: v1
kind: Service
metadata:
  name: {{ include "robocaps.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "robocaps.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
