# Benchmarking Protocol

- Datasets: LineMOD (+ Occlusion), optional YCB-Video
- Metrics: ADD/ADD-S, reprojection error, AUC
- Seeds and splits documented in `configs/linemod.yaml`
- Log outputs to `research/experiments/results/linemod/*.jsonl`
